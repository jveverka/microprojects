server:
  port: ${PORT:8085}
  servlet:
    context-path: ${CONTEXT_PATH:/events}

app:
  elastic-host: ${ELASTIC_HOST:localhost}
  elastic-port: ${ELASTIC_PORT:9200}
  elastic-user: ${ELASTIC_USER:elastic}
  elastic-pass: ${ELASTIC_PASS:secret}
